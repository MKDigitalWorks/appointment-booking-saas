{
  "root": true,
  "extends": ["next/core-web-vitals"],
  "ignorePatterns": [
    "node_modules/",
    ".next/",
    "playwright-report/",
    "test-results/",
    "blob-report/",
    "dist/",
    "coverage/"
  ],
  "overrides": [
    {
      "files": ["**/*.ts", "**/*.tsx", "**/*.js", "**/*.jsx"],
      "rules": {
        // Du kannst hier bei Bedarf später Regeln ergänzen
      }
    },
    {
      "files": ["tests/**/*", "**/*.spec.ts", "**/*.spec.tsx"],
      "rules": {
        // Tests etwas lockerer halten
        "@next/next/no-html-link-for-pages": "off"
      }
    }
  ]
}
